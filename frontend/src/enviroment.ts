let host_url = "http://localhost";
const port = ':3000'
export const url = host_url + port;

export const environment = {
    production: false,
    user: {
        signup: '/signup',
        auth: '/auth'
    },
};